{
  "name": "biswap_vaults",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ n/biswap_vaults",
    "create-local": "graph create --node http://localhost:8020/ n/biswap_vaults",
    "remove-local": "graph remove --node http://localhost:8020/ n/biswap_vaults",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 n/biswap_vaults"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
